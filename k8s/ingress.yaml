apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deepagents-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: api.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: deepagents-service
            port:
              number: 80
